% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/draw-key-icon.R
\name{draw_key_icon}
\alias{draw_key_icon}
\title{Legend key glyph: an icon}
\usage{
draw_key_icon(data, params, size)
}
\arguments{
\item{data}{A single row data frame containing the scaled aesthetics to
display in this key}

\item{params}{A list of additional parameters supplied to the geom.}

\item{size}{Width and height of key in mm.}
}
\value{
A grob.
}
\description{
A \code{\link[ggplot2:draw_key]{ggplot2::draw_key()}} function that draws the icon itself as the
legend key glyph, instead of a generic point/rect. This is
\code{\link[=geom_icon]{geom_icon()}}'s default \code{key_glyph}; set \code{key_glyph = draw_key_icon} on
another geom to borrow it.
}
\examples{
library(ggplot2)

# bundled placeholder shapes; a real pack (e.g. icons::fontawesome) works the same
shapes <- icons::icon_set(system.file("icons", package = "ggicons"))

# borrowed onto geom_point() so its legend key shows the mapped icon
df <- data.frame(x = 1:3, y = c(1, 3, 2), cat = c("a", "b", "c"))
ggplot(df, aes(x, y, colour = cat, icon = cat)) +
  geom_point(size = 10, key_glyph = draw_key_icon) +
  scale_icon_manual(values = c(shapes$square, shapes$circle, shapes$triangle))
}
