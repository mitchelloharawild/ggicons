% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotation-icon.R
\name{annotation_icon}
\alias{annotation_icon}
\title{Annotate a plot with icons at fixed positions}
\usage{
annotation_icon(
  icon,
  x,
  y,
  colour = "black",
  fill = NA,
  size = 6,
  alpha = NA,
  angle = 0,
  size.unit = "mm"
)
}
\arguments{
\item{icon}{An icon vector from the
\href{https://github.com/mitchelloharawild/icons}{icons} package. Character
ids are not resolved; pass an actual icon vector.}

\item{x, y}{The position(s) to draw the icon(s) at, in data coordinates.}

\item{colour, fill}{Fill colour for the icon; \code{fill} wins when both are
set.}

\item{size}{The icon's width/height, in \code{size.unit}.}

\item{alpha}{Transparency, from 0 (fully transparent) to 1 (opaque).}

\item{angle}{Rotation, in degrees.}

\item{size.unit}{The unit in which \code{size} is interpreted, one of \code{"mm"}
(default), \code{"pt"}, \code{"cm"}, \code{"in"}, or another \code{\link[grid:unit]{grid::unit()}}-compatible
absolute unit.}
}
\value{
A ggplot2 layer.
}
\description{
\code{annotation_icon()} draws one or more icons at fixed \verb{(x, y)} positions,
the \code{icon} analogue of \code{\link[ggplot2:annotation_custom]{ggplot2::annotation_custom()}} /
\code{\link[ggplot2:annotation_raster]{ggplot2::annotation_raster()}} — a single call that adds icons directly,
without a data frame or \code{aes()} mapping. Unlike those, though,
\code{annotation_icon()} \emph{does} train the panel's position scales: a
fixed-position icon is more usefully treated like a point annotation
(e.g. \code{\link[ggplot2:annotate]{ggplot2::annotate()}}) than like an arbitrary grob pinned to the
panel's corners, so placing one outside the data's range still expands
the axes to fit it.
}
\details{
\code{icon}, \code{x}, \code{y} and the style arguments are all recycled to a common
length, so a single call can place several icons at once (e.g.
\code{annotation_icon(icon = c(rocket, star), x = c(1, 2), y = c(1, 1))}).
}
\section{Aesthetics}{

\code{annotation_icon()} sets the same per-icon properties \code{\link[=geom_icon]{geom_icon()}}
understands as aesthetics, but as plain arguments instead:
\itemize{
\item \strong{\code{icon}} (required): an icon vector from the
\href{https://github.com/mitchelloharawild/icons}{icons} package — an icon
object (e.g. from \code{\link[icons:read_icon]{icons::read_icon()}} or
\code{icons::fontawesome$solid$rocket}) or an \code{\link[icons:icon_find]{icons::icon_find()}} result.
\item \code{x}, \code{y}: the position(s) to draw at, in data coordinates.
\item \code{colour}/\code{fill}: both map to the icon's single fill colour (icons
don't have a separate border) — \code{fill} wins when both are set.
\item \code{alpha}
\item \code{size}: the icon's width/height, in \code{size.unit} (default millimetres).
\item \code{angle}: rotation, in degrees.
}

\code{stroke}/\code{linewidth} don't apply — icons are filled shapes, not framed
ones.
}

\examples{
library(ggplot2)
library(icons)

ggplot(data.frame(x = 1:3, y = c(1, 3, 2)), aes(x, y)) +
  geom_point() +
  annotation_icon(
    icon = fontawesome$solid$rocket,
    x = 2, y = 3, size = 12, colour = "steelblue"
  )
}
\seealso{
\code{\link[=geom_icon]{geom_icon()}}
}
