% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scale-icon.R
\name{scale_icon_identity}
\alias{scale_icon_identity}
\title{Use icon values as-is}
\usage{
scale_icon_identity(
  name = ggplot2::waiver(),
  ...,
  guide = "none",
  aesthetics = "icon"
)
}
\arguments{
\item{name}{The name of the scale. Used as the axis or legend title. If
\code{waiver()}, the default, the name of the scale is taken from the first
mapping used for that aesthetic. If \code{NULL}, the legend title will be
omitted.}

\item{...}{Other arguments passed on to \code{\link[ggplot2:discrete_scale]{discrete_scale()}} or
\code{\link[ggplot2:continuous_scale]{continuous_scale()}}}

\item{guide}{Guide to use for this scale. Defaults to \code{"none"}.}

\item{aesthetics}{Character string(s) naming the aesthetic(s) this scale
works with, defaulting to \code{"icon"}.}
}
\value{
A ggplot2 scale.
}
\description{
\code{scale_icon_identity()} is for the common case where the \code{icon}
aesthetic is already mapped to icon values (an icon vector from the
\href{https://github.com/mitchelloharawild/icons}{icons} package), so the data
is passed through unchanged rather than looked up in a palette. No legend
is drawn by default, as with \code{\link[ggplot2:scale_shape_identity]{ggplot2::scale_shape_identity()}}.
}
\details{
Requesting a legend (\code{guide = "legend"}) isn't supported here: ggplot2's
discrete scale training only recognises character/factor data, not icon
vectors. Use \code{\link[=scale_icon_manual]{scale_icon_manual()}} instead when a legend is needed.
}
\seealso{
\code{\link[=scale_icon_manual]{scale_icon_manual()}}
}
